# OS
.DS_Store
Thumbs.db

# IDE
/.idea
/.vscode
/.fleet
/.zed

# Logs
*.log

# Env (per-subproject .gitignore handles app-specific .env)
.env
.env.local

# Meilisearch local data store (created when an agent or test runs meilisearch from this folder)
data.ms/

# Reference docs that are kept around but not committed deeper than this snapshot
# DOCS/ stays included — it's the architecture + mockup reference for the whole team

# Local deploy artifacts (SSH keypair lives here; never committed)
.deploy-keys/

